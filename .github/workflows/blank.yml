# This is a basic workflow to help you get started with Actions

name: Playground

on:
  workflow_dispatch:

jobs:
  playground:
    environment: test
    runs-on: self-hosted
    steps:
      - name: helloWorld
        shell: bash
        run: printenv
